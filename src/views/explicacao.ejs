<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= conteudo.titulo %></title>
    <link rel="stylesheet" href="/css/explicacao.css" />
    <style>
        /* Botões de navegação no rodapé */
        .nav-buttons {
            display: flex;
            justify-content: space-between;
            align-items: center;
            width: 100%;
            position: fixed;
            bottom: 30px;
            left: 0;
            padding: 0 50px;
            z-index: 1000;
        }
        
        /* Estilos para passos numerados */
        .steps-container {
            display: flex;
            flex-direction: column;
            gap: 20px;
            margin-top: 20px;
        }
        
        .step {
            display: flex;
            gap: 15px;
            padding: 15px;
            background: #f5f5f5;
            border-radius: 8px;
            border-left: 4px solid #ff6b35;
        }
        
        .step-number {
            flex-shrink: 0;
            width: 40px;
            height: 40px;
            background: #ff6b35;
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            font-size: 18px;
        }
        
        .step-content {
            flex: 1;
            padding-top: 2px;
            line-height: 1.6;
        }
    </style>
</head>
<body>
    <div class="container">
        <header class="header">
            <div class="browser-bar">
                <div class="browser-dots">
                    <div class="dot orange"></div>
                    <div class="dot orange"></div>
                    <div class="dot orange"></div>
                </div>
            </div>
            <h1 class="main-title"><%= conteudo.titulo %></h1>
        </header>
 
        <main class="content">
            <!-- TEXTO EXPLICATIVO -->
            <section class="rules-section" aria-labelledby="sec-explicacao">
                <div class="rule-item" data-rule="1">
                    <div class="rule-text">
                        <h3 id="sec-explicacao">
                            <% 
                                // Verifica se o texto contém quebras de linha (passos)
                                const linhas = conteudo.texto.split('\n').filter(l => l.trim().length > 0);
                                const temPassos = linhas.length > 1 && conteudo.nivel >= 2;
                            %>
                            
                            <% if (temPassos) { %>
                                <!-- Renderiza como passos numerados -->
                                <div class="steps-container">
                                    <% linhas.forEach((passo, index) => { %>
                                        <div class="step">
                                            <div class="step-number"><%= index + 1 %></div>
                                            <div class="step-content">
                                                <%= passo.trim().replace(/^\d+\s*-\s*/, '') %>
                                            </div>
                                        </div>
                                    <% }); %>
                                </div>
                            <% } else { %>
                                <!-- Renderiza como texto simples (nível 1) -->
                                <p><%= conteudo.texto %></p>
                            <% } %>
                        </h3>
                    </div>
                </div>
            </section>
 
            <!-- IMAGENS ILUSTRATIVAS -->
            <aside class="image-section" aria-label="Imagens ilustrativas">
                <% if (conteudo.imagem && conteudo.imagem.trim() !== '') { %>
                    <% 
                        const imagens = conteudo.imagem.split('|');
                        const temDuasImagens = imagens.length > 1;
                    %>
                    <% if (temDuasImagens) { %>
                        <!-- Duas imagens lado a lado -->
                        <div style="display: flex; gap: 20px; justify-content: center; align-items: center; width: 100%;">
                            <img src="/img/<%= imagens[0] %>" alt="Ilustração" class="image-item" style="max-width: 45%; height: auto;" />
                            <img src="/img/<%= imagens[1] %>" alt="Ilustração" class="image-item" style="max-width: 45%; height: auto;" />
                        </div>
                    <% } else { %>
                        <!-- Uma imagem única -->
                        <img src="/img/<%= conteudo.imagem %>" alt="Ilustração" class="image-item" />
                    <% } %>
                <% } %>
            </aside>
        </main>
 
        <footer class="footer">
            <div class="nav-buttons">
                <button class="learn-more-btn" id="prevPageBtn" type="button" onclick="voltarPagina()" <% if (!temAnterior) { %>disabled<% } %>>
                    <span>Página Anterior</span>
                    <div class="btn-glow"></div>
                </button>
 
                <button class="learn-more-btn" id="nextPageBtn" type="button" onclick="proximaPagina()">
                    <span>Próxima Página</span>
                    <div class="btn-glow"></div>
                </button>
            </div>
        </footer>
    </div>
 
    <script>
        const paginaAtual = <%= ordem %>;
        const temProxima = <%= temProxima %>;
        const temAnterior = <%= temAnterior %>;

        function voltarPagina() {
            if (paginaAtual > 1) {
                const ordemAnterior = <%= ordem %> - 1;
                window.location.href = `/pagina/${ordemAnterior}`;
            }
        }

        function proximaPagina() {
            const proximaOrdem = <%= ordem %> + 1;
            window.location.href = `/pagina/${proximaOrdem}`;
        }
    </script>
</body>
</html>
